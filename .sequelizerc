const path = require('path');

module.exports = {
    config: path.resolve('migration_files/config', './config.json'),
    'models-path': path.resolve('models'),
    'migrations-path': path.resolve('migration_files/migrations'),
  };
